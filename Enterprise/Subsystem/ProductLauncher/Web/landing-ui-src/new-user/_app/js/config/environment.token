//  Environment Constant

(function (angular) {
    "use strict";

    var envConfig = {};

    envConfig = {
        name: "token",
		landingAPI: "#{LandingAPIUri}#"
	};

	Object.freeze(envConfig);

	angular
		.module("new-user")
		.constant("ENV", envConfig);
})(angular); 