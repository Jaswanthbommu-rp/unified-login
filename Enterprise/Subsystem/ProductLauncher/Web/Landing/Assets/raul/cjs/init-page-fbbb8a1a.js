'use strict';

function initPage(name, element = true) {
    console.log(name, element);
    //const RAUL = (window as any).RAUL
    //if (element) {
    //  RAUL.commands.pageHeader(name, ['RAUL', 'Elements', name])
    //} else {
    //  RAUL.commands.pageHeader(name, ['RAUL', name])
    //}
    //document.querySelector('main').scrollTop = 0
}

exports.initPage = initPage;
